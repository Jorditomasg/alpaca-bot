services:
  alpaca-bot:
    container_name: alpaca-bot
    build: .
    restart: unless-stopped
    ports:
      - "7080:7080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
