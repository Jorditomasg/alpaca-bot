# Credentials & state — NEVER commit these
.env
*.json
# Allow committed test fixtures
!tests/fixtures/**/*.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Playwright (not used anymore but just in case)
.playwright/

# OS
.DS_Store
Thumbs.db

# SDD / Agent infra (regenerable, machine-specific paths)
.atl/
